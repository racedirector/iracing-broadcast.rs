# JS tooling for `iracing-broadcast-wasm`

This folder hosts JavaScript-side tooling for the WASM interface scaffold.

## Package manager

Use `pnpm` (not `npm`).

## Commands

```bash
pnpm install
pnpm lint
pnpm test
```

Vitest is used as the test runner, and ESLint includes Vitest recommended lint rules for test files.
